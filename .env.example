# Triage creates .local/triage.env automatically.
# These names document the local-only settings; do not commit real values.
TRIAGE_SA_PASSWORD=replace-during-initialization
TRIAGE_APP_PASSWORD=replace-during-initialization
TRIAGE_ADMIN_PASSWORD=replace-during-initialization
TRIAGE_REVIEWER_PASSWORD=replace-during-initialization

